pipeline {
    agent any
    stages {
        stage ('Destroy GCP cluster') {
            steps {
                sh 'cd ~/workspace/create-gcp-infrastructure'
                sh 'terraform destroy -auto-approve'
            }
        }
    }
}