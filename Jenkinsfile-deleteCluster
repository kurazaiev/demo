pipeline {
    agent any
    stages {
        stage ('Destroy GCP cluster') {
            steps {
                sh 'cd /var/lib/jenkins/workspace/create-gcp-infrastructure/'
                sh 'terraform destroy'
            }
        }
    }
}