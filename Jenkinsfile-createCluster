pipeline {
    agent any
    stages {
        stage ('Create GCP cluster') {
            agent any
            steps {
                sh  'terraform init  -input=false'
                 sh 'terraform apply -input=false -auto-approve'
            }
        }
    }
}